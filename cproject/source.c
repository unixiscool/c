#include <stdio.h>
#define LOWER 0 /* нижняя граница таблицы */
#define UPPER 300 /* верхняя граница */ 
#define STEP 20 /* верхняя граница */

int main() {
  int fahr;
  //for (fahr = 0; fahr <= 300; fahr = fahr + 20) {
  // printf("%3d %6.2f\n", fahr, (5.0/9.0)*(fahr-32));
  //}

  //for (fahr = 300; fahr >= 0; fahr = fahr - 20) {
  //  printf("%3d %6.2f\n", fahr, (5.0/9.0)*(fahr-32));
  //}

  //int c;
  //c = getchar();
  //while (c != EOF) {
  //putchar(c);
  //c = getchar();
  //}
  //while (c = getchar() != EOF)
    //printf("%d\n", c); // 0 or 1

  //while ((c = getchar()) != EOF)
  //putchar(c); //all cool
}
